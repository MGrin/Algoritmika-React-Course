import React from 'react'

export default class ProductRow extends React.Component {
  render() {
    const {
      product,
      deleteProduct,
    } = this.props

    return (
      /**
       * Компонент должен отрисовывать строчку продукта
       * с названием продукта, ценой и кнопкой удаления продукта
       */
    )
  }
}